import React from 'react';
import PropTypes from 'prop-types';
import {Button} from 'react-bootstrap';

const JoinAsSpeaker = () => (
  <Button variant="outline-secondary" size="lg">Join As Speaker</Button>
);

JoinAsSpeaker.propTypes = {};

JoinAsSpeaker.defaultProps = {};

export default JoinAsSpeaker;
